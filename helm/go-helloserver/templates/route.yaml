
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "demo-sreez-video.fullname" . }}
spec:
  host: {{ .Values.route.host }}
  port:
    targetPort: http
  to:
    kind: Service
    name: {{ include "demo-sreez-video.fullname" . }}

